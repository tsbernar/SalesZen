<div class="jumbotron">
	<div class="container">
		<p>First name: <br />
		<%= @contact.first_name %></p>
		<p>Last name: <br />
		<%= @contact.last_name %></p>
		<p>Email: <br />
		<%= @contact.email %></p>
		<p>Address: <br />
		<%= @contact.address %></p>
		<p>Assigned to: <br />
		<%= @contact.user.email %> </p>
	</div>
	<%= console %>
</div>